# Estructura del repositorio

- `app/` centraliza autenticación, configuración, controladores y vistas.
- `public/` expone el punto de entrada web y los assets.
- `database/` mantiene el esquema MySQL.
- `docs/` guarda evidencias, QA y reportes.
